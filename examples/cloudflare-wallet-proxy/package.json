{
  "name": "@hermes-agent/cloudflare-wallet-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker wallet secret proxy for radius-cli proxy wallet provider.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@coinbase/cdp-sdk": "^1.51.0",
    "@getpara/server-sdk": "3.0.0",
    "@getpara/viem-v2-integration": "3.0.0",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260601.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.131.0"
  }
}
